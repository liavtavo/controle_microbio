CREATE DATABASE bacterio_upnp WITH OWNER thomas;
\connect bacterio_upnp
\q
\d
\d
\d
drop database bacterio_upnp;
drop database bacterio_upnp 
;
drop database bacterio_upnp;
;
drop database bacterio_upnp ;
\d
ALTER USER postgres WITH PASSWORD 'admin@pgsql';
SELECT prelevements.id, prelevements.date_prelev, point, description, classes, type, dispositif FROM prelevements, points_prelev, disp_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_disp=disp_prelev.id AND points_prelev.id_class=limites_classes.id;
SELECT resultats.id, prelevements.date_prelev, point, description, classe FROM resultats, prelevements, points_prelev, limites_classes, date_res, tel, sign, ufc, germe WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
\d resultats
SELECT resultats.id, prelevements.date_prelev, point, description, classe, date_res, tel, sign, ufc, germe  FROM resultats, prelevements, points_prelev, limites_classes WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
alter table planning_prelev add constraint id_point foreign key id_point references points_prelev(id);
\d
\d planning_prelev
select id_point, point, description from planning_prelev, points_prelev where id_point=id;
alter table planning_prelev add constraint id_point foreign key (id_point) references points_prelev(id);
\d planning_prelev
select id_point, point, description from planning_prelev, points_prelev where id_point=id;
CREATE ROLE pharmacien LOGIN PASSWORD 'zac';
\dp
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO pharmacien;
select * from points_prelev;
SELECT point, dispositif, type, description FROM disp_prelev, limites_classes, points_prelev WHERE points_prelev.id_disp=disp_prelev.id AND points_prelev.id_class=limites_classes.id AND description LIKE 'automate% OR description LIKE 'balance%';
select * from points_prelev;

'
;
SELECT point, dispositif, type, description FROM disp_prelev, limites_classes, points_prelev WHERE points_prelev.id_disp=disp_prelev.id AND points_prelev.id_class=limites_classes.id AND description LIKE 'automate%' OR description LIKE 'balance%';
SELECT point, dispositif, type, description FROM disp_prelev, limites_classes, points_prelev WHERE points_prelev.id_disp=disp_prelev.id AND points_prelev.id_class=limites_classes.id AND (description LIKE 'automate%' OR description LIKE 'balance%');
SELECT point, dispositif, type, description FROM disp_prelev, limites_classes, points_prelev WHERE points_prelev.id_disp=disp_prelev.id AND points_prelev.id_class=limites_classes.id AND classe LIKE 'A' AND (DESCRIPTION NOT LIKE 'automate%' OR DESCRIPTION NOT LIKE 'balance%');
SELECT point, dispositif, type, description FROM disp_prelev, limites_classes, points_prelev WHERE points_prelev.id_disp=disp_prelev.id AND points_prelev.id_class=limites_classes.id AND classe LIKE 'A' AND (DESCRIPTION NOT LIKE 'automate%' AND  DESCRIPTION NOT LIKE 'balance%');
SELECT jour, classe, type, point, description FROM planning_prelev, limites_classes, points_prelev, jours_prelev WHERE jours_prelev.id=planning_prelev.id_jour AND points_prelev.id=planning_prelev.id_point AND points_prelev.id_class=limites_classes.id;
CREATE VIEW planning AS SELECT jour, classe, type, point, description FROM planning_prelev, limites_classes, points_prelev, jours_prelev WHERE jours_prelev.id=planning_prelev.id_jour AND points_prelev.id=planning_prelev.id_point AND points_prelev.id_class=limites_classes.id;
select * from planning
;
\dv
\s
\s 'thomas'
\s '/media/www-dev-local/projet-LAPP/sql_20180501'
\set HISTFILE ~/psql_history
\set HISTSIZE 5000
CREATE VIEW planning AS SELECT jour, classe, type, point, description FROM planning_prelev, limites_classes, points_prelev, jours_prelev WHERE jours_prelev.id=planning_prelev.id_jour AND points_prelev.id=planning_prelev.id_point AND points_prelev.id_class=limites_classes.id;
SELECT * FROM crosstab(planning) AS (jour text, classe text, Gants text, Air text, Surface text);
SELECT * FROM crosstab('SELECT jour, classe, type, point, description FROM planning_prelev, limites_classes, points_prelev, jours_prelev WHERE jours_prelev.id=planning_prelev.id_jour AND points_prelev.id=planning_prelev.id_point AND points_prelev.id_class=limites_classes.id') AS (jour text, classe text, Gants text, Air text, Surface text);
select * from planning_prelev where jour=A;
select * from planning_prelev where jour='A';
select * from planning_prelev where jour like 'A';
\d planning_prelev
select * from planning where jour like 'A';
select * from planning where jour like A;
select * from planning where jour like 'lundi';
select * from planning where jour like 'lundi' and classe like 'A' and type like 'surface';
select * from planning where jour like 'lundi' and classe like 'A' and type like 'Surface';
SELECT prelevements.id AS 'No', prelevements.date_prelev AS 'Date du prélèvement', point, description, classe FROM prelevements, points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
SELECT prelevements.id AS "No", prelevements.date_prelev AS "Date du prélèvement", point, description, classe FROM prelevements, points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
SELECT prelevements.id AS \"No\", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS \"No\", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS 'No', prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS "Date du prélèvement", point, description, classe FROM prelevements, points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
SELECT prelevements.id AS 'No', prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' HEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS 'No', prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' HEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS 'No', prelevements.date_prelev AS "Date du prélèvement", point, description, classe FROM prelevements, points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' HEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' THEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '$date_debut' AND '$date_fin' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-01-05' AND classe LIKE '$classe' AND type LIKE '$type' AND conformité LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-01-05' AND classe LIKE '$classe' AND type LIKE '$type' AND 'conformité' LIKE '$conf' AND téléphoné LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-01-05' AND classe LIKE '$classe' AND type LIKE '$type' AND 'conformité' LIKE '$conf' AND 'téléphoné' LIKE '$tel' AND signé LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-01-05' AND classe LIKE '$classe' AND type LIKE '$type' AND 'conformité' LIKE '$conf' AND 'téléphoné' LIKE '$tel' AND'signé' LIKE '$sign';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-01-05' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui' AND 'téléphoné' LIKE 'non' AND'signé' LIKE 'oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui' AND 'téléphoné' LIKE 'non' AND'signé' LIKE 'oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui' AND 'téléphoné' LIKE 'non' AND'signé' LIKE 'oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui' AND 'téléphoné' LIKE 'non' AND'signé' LIKE 'oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui' AND 'téléphoné' LIKE 'non';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE '%';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité'='oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE oui;
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE '%oui';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE '%';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'non';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' HVING 'conformité' LIKE 'non';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' HAVING 'conformité' LIKE 'non';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' HVING 'conformité' LIKE 'ufc<limite';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND 'conformité' LIKE 'ufc<limite';
SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id AND prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND conformité LIKE 'oui';
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id)x WHERE x.prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND x.classe LIKE 'A' AND x.type LIKE 'Gants' AND x.conformité LIKE 'oui' AND x.téléphoné LIKE 'non' AND x.signé LIKE 'oui';
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id) WHERE prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND conformité LIKE 'oui' AND téléphoné LIKE 'non' AND signé LIKE 'oui';
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id) AS foo  WHERE prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND conformité LIKE 'oui' AND téléphoné LIKE 'non' AND signé LIKE 'oui';
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'conforme' WHEN ufc>=limite THEN 'non conforme' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id)AS x WHERE x.prelevements.date_prelev BETWEEN '2018-01-01' AND '2018-03-03' AND x.classe LIKE 'A' AND x.type LIKE 'Gants' AND x.conformité LIKE 'oui' AND x.téléphoné LIKE 'non' AND x.signé LIKE 'oui';
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id)AS x WHERE x.date BETWEEN '2018-01-01' AND '2018-03-03' AND x.classe LIKE 'A' AND x.type LIKE 'Gants' AND x.conformité LIKE 'oui' AND x.téléphoné LIKE 'non' AND x.signé LIKE 'oui';
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id)AS x WHERE x.date BETWEEN '2018-01-01' AND '2018-03-03' AND x.classe LIKE 'A' AND x.type LIKE 'Gants' AND x.conformité LIKE 'oui' AND x.téléphoné LIKE 'non' AND x.signé LIKE 'oui';
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id)AS x WHERE x.date BETWEEN '2018-01-01' AND '2018-03-03' AND x.classe LIKE 'A' AND x.type LIKE 'Gants' AND x.conformité LIKE 'oui' AND x.téléphoné LIKE 'non' AND x.signé LIKE 'oui';
CREATE VIEW rp AS SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id;
SELECT * FROM rp  WHERE date BETWEEN '2018-01-01' AND '2018-03-03' AND classe LIKE 'A' AND type LIKE 'Gants' AND conformité LIKE 'oui' AND téléphoné LIKE 'non' AND signé LIKE 'oui';
SELECT point, type, description, classe, COUNT(prelevements.id) AS nb FROM (SELECT prelevements.id, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id) AS x WHERE x.classe LIKE '$classe' AND x.type LIKE '$type' AND x.conformité LIKE '$conf' GROUP BY point, type, description, classe ORDER BY nb DESC;
SELECT point, type, description, classe, COUNT(prelevements.id) AS nb FROM (SELECT prelevements.id, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id) AS x WHERE x.classe LIKE '$classe' AND x.type LIKE '$type' AND x.conformité LIKE '$conf' GROUP BY point, type, description, classe ORDER BY nb DESC;
SELECT point, type, description, classe, COUNT(idp) AS nb FROM (SELECT prelevements.id AS idp, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité FROM prelevements, points_prelev, limites_classes, resultats WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND resultats.id_prelev=prelevements.id) AS x WHERE x.classe LIKE '$classe' AND x.type LIKE '$type' AND x.conformité LIKE '$conf' GROUP BY point, type, description, classe ORDER BY nb DESC;
SELECT germe, type, description, classe, COUNT(id.resultats) AS nb FROM resultats, classe, points_prelev WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=poinst_prelev.id AND points_prelev.id_class=limites_classes.id GROUP BY germe, type, description, classe ORDER BY nb DESC;
SELECT germe, type, description, classe, COUNT(id.resultats) AS nb FROM resultats, limites_classes, points_prelev WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=poinst_prelev.id AND points_prelev.id_class=limites_classes.id GROUP BY germe, type, description, classe ORDER BY nb DESC;
SELECT germe, type, description, classe, COUNT(resultats.id) AS nb FROM resultats, limites_classes, points_prelev WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=poinst_prelev.id AND points_prelev.id_class=limites_classes.id GROUP BY germe, type, description, classe ORDER BY nb DESC;
SELECT germe, type, description, classe, COUNT(resultats.id) AS nb FROM resultats, prelevements, limites_classes, points_prelev WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=poinst_prelev.id AND points_prelev.id_class=limites_classes.id GROUP BY germe, type, description, classe ORDER BY nb DESC;
SELECT germe, type, description, classe, COUNT(resultats.id) AS nb FROM resultats, prelevements, limites_classes, points_prelev WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id GROUP BY germe, type, description, classe ORDER BY nb DESC;
SELECT germe, COUNT(resultats.id) AS nb FROM resultats, prelevements, limites_classes, points_prelev WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND germe NOT LIKE 'NA' AND germe IS NOT NULL GROUP BY germe, type, description, classe ORDER BY nb DESC;
SELECT germe, COUNT(resultats.id) AS nb FROM resultats, prelevements, limites_classes, points_prelev WHERE resultats.id_prelev=prelevements.id AND prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND germe NOT LIKE 'NA' AND germe NOT LIKE '' GROUP BY germe, type, description, classe ORDER BY nb DESC;
INSERT INTO prelevements (date_prelev, id_point) VALUES (2018/05/08, 3);
INSERT INTO prelevements (date_prelev, id_point) VALUES ('2018/05/08', 3);
INSERT INTO prelevements (date_prelev, id_point) VALUES ('2018/05/08', 3);
INSERT INTO prelevements (DEFAULT, date_prelev, id_point) VALUES ('2018/05/08', 3);
INSERT INTO prelevements (id, date_prelev, id_point) VALUES (DEFAULT, '2018/05/08', 3);
\d prele
\d prelevements
\d prelevements
INSERT INTO prelevements (id, date_prelev, id_point) VALUES (DEFAULT, '2018/05/08', 3);
INSERT INTO prelevements (date_prelev, id_point) VALUES ('2018/05/08', '3');
INSERT INTO prelevements (id, date_prelev, id_point) VALUES (726, '2018/05/08', 3);
\d prelevements
select * from prelevements
;
select * from prelevements order by id desc;
select * from prelevements order by id desc;
INSERT INTO prelevements (id, date_prelev, id_point) VALUES (782, '2018/05/08', 3);
INSERT INTO prelevements (date_prelev, id_point) VALUES ('2018/05/08', 5);
SELECT MAX (id) FROM prelevements;
SELECT nextval('prelevements.id_sequence');
SELECT nextval('id_sequence');
SELECT nextval('id_sequence');
SELECT nextval('prelevements_sequence');
SELECT nextval('prelevements');
SELECT nextval('prelevements.id');
\d
\d
SELECT nextval('prelevements_id_seq');
SELECT MAX (id) FROM prelevements;
SELECT setval('prelevements_id_seq', (SELECT MAX(id) FROM prelevements)+1);
SELECT nextval('prelevements_id_seq');
INSERT INTO prelevements (date_prelev, id_point) VALUES ('2018/05/08', 5);
SELECT nextval('prelevements_id_seq');
SELECT MAX (id) FROM resultats;
SELECT nextval('resultats_id_seq');
SELECT MAX (id) FROM planning;
INSERT INTO prelevements (date_prelev, id_point) VALUES ('2018/05/08', 12);
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO pharmacien;
INSERT INTO prelevements (date_prelev, id_point) VALUES ('2018/05/08', 12);
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' ELSE CASE IS NULL END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' ELSE WHEN ufc IS NULL END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' WHEN ufc IS NULL END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' ELSE 1 END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' ELSE TRUE END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-05-08' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' ELSE TRUE END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NOT NULL THEN x.conformité LIKE '%' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' WHEN ufc IS NULL THEN TRUE END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN x.conformité OR x.téléphoné OR x.signé NOT LIKE '%' THEN x.conformité LIKE 'oui' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' ELSE TRUE END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NULL THEN TRUE ELSE x.conformité LIKE 'oui' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN ufc IS NULL THEN ufc IS NULL ELSE x.conformité LIKE 'oui' AND x.téléphoné LIKE '%' AND x.signé LIKE '%' END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN x.conformité NOT LIKE '%' THEN ufc IS NOT NULL WHEN x.téléphoné NOT LIKE '%' THEN ufc IS NOT NULL WHEN x.signé NOT LIKE '%' THEN ufc IS NOT NULL ELSE ufc IS NULL END ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN x.conformité NOT LIKE '%' THEN ufc IS NOT NULL WHEN x.téléphoEND ORDER BY "No" DESC;
SELECT * FROM (SELECT prelevements.id AS "No", prelevements.date_prelev AS date, point, type, description, classe, ufc, limite, CASE WHEN ufc<limite THEN 'oui' WHEN ufc>=limite THEN 'non' ELSE NULL END AS conformité, germe, CASE WHEN tel IS TRUE THEN 'oui' WHEN tel IS FALSE THEN 'non' ELSE 'non renseigné' END AS téléphoné, CASE WHEN sign IS TRUE THEN 'oui' WHEN sign IS FALSE THEN 'non' ELSE 'non renseigné' END AS signé FROM prelevements LEFT OUTER JOIN resultats ON (resultats.id_prelev=prelevements.id), points_prelev, limites_classes WHERE prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id) AS x WHERE x.date BETWEEN '2018-02-02' AND '2018-04-30' AND x.classe LIKE '%' AND x.type LIKE '%' AND CASE WHEN x.conformité NOT LIKE '%' THEN ufc IS NOT NULL WHEN x.téléphoné NOT LIKE '%' THEN ufc IS NOT NULL WHEN x.signé NOT LIKE '%' THEN ufc IS NOT NULL END ORDER BY "No" DESC;
SELECT point, id FROM points_prelev WHERE id LIKE '$id_point';
SELECT point, id FROM points_prelev WHERE id LIKE '1';
SELECT point, id FROM points_prelev WHERE id=1;
SELECT point, id FROM points_prelev WHERE id=36;
select * from prelevements where date = '2018/02/05';
select * from prelevements where date_prelev = '2018/02/05';
select * from prelevements where date_prelev = '2018/05/02';
select prelevements.id, classe, type, point, description, limite where prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
select prelevements.id, classe, type, point, description, limite from limites_classes, points_prelev, prelevements where prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id;
select prelevements.id, classe, type, point, description, limite from limites_classes, points_prelev, prelevements where prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id order by prelevements.id desc;
select prelevements.id, point, description, classe, type, limite from limites_classes, points_prelev, prelevements where prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id order by prelevements.id desc;
select prelevements.id, point, description, classe, type, limite from limites_classes, points_prelev, prelevements where prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND date_prelev='2018-O5-03' order by prelevements.id desc;
select prelevements.id, point, description, classe, type, limite from limites_classes, points_prelev, prelevements where prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND date_prelev='2018/O5/03' order by prelevements.id desc;
select prelevements.id, point, description, classe, type, limite from limites_classes, points_prelev, prelevements where prelevements.id_point=points_prelev.id AND points_prelev.id_class=limites_classes.id AND date_prelev='2018-05-08' order by prelevements.id desc;
\d resultats
ALTER TABLE resultats ADD COLUMN type_rendu;
\d resultats
ALTER TABLE resultats ADD COLUMN type_rendu;
ALTER TABLE resultats ADD COLUMN type_rendu
;
ALTER TABLE resultats ADD COLUMN type_rendu varchar(10);
\d resultats
ALTER TABLE resultats drop column tel;
ALTER TABLE resultats drop column tel CASCADE;
\d resultats
ALTER TABLE resultats drop column sign CASCADE;
\d resultats
UPDATE resultats SET type_rendu = 'téléphoné' WHERE type_rendu IS NULL; [A
select * from resultats order by date_res desc;
UPDATE resultats SET type_rendu = 'signé' WHERE date_res > '2018-04-01';
select * from resultats order by date_res desc;
\d resultats
bvefnb
;
UPDATE resultats SET type_rendu = 'téléphoné' WHERE date_res >= '2018-04-01';
UPDATE resultats SET type_rendu = 'signé' WHERE date_res < '2018-04-01';
select * from resultats limit 30;
select * from resultats limit 30 order by id;
select * from resultats order by id, limit 30;
select * from resultats order by id and limit 30;
select * from resultats order by id;
select * from resultats order by id, limit 30;
select * from resultats order by id limit 30;
select * from resultats order by id desc limit 30;
select * from resultats order by id desc limit 50;
select * from resultats order by id desc;
UPDATE resultats SET type_rendu = 'signé' WHERE germe<>'NA' AND germe IS NOT NULL;
select * from resultats order by id desc;
UPDATE resultats SET type_rendu = 'téléphoné' WHERE germe IS NULL;
UPDATE resultats SET type_rendu = 'téléphoné' WHERE germe LIKE '';
select * from resultats order by id desc;
\s /media/www-dev-local/projet-LAPP/modif_table_resultats.txt
\s modif_table_resultats.txt
